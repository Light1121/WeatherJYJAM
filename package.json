{
  "name": "weatherjyjam-monorepo",
  "private": true,
  "devDependencies": {
    "husky": "^9.1.7"
  },
  "scripts": {
    "prepare": "husky",
    "fe:dev": "cd _Frontend && npm run dev",
    "fe:build": "cd _Frontend && npm run build",
    "fe:preview": "cd _Frontend && npm run preview",
    "fe:precommit": "cd _Frontend && npm run pre-commit"
  }
}