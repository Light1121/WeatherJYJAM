{
  "name": "weatherjyjam-monorepo",
  "private": true,
  "devDependencies": {
    "husky": "^9.1.7"
  },
  "scripts": {
    "prepare": "husky",
    "frontend:dev": "cd Frontend && npm run dev",
    "frontend:build": "cd Frontend && npm run build",
    "frontend:preview": "cd Frontend && npm run preview",
    "frontend:precommit": "cd Frontend && npm run pre-commit"
  }
}
